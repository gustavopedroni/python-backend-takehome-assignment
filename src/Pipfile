[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
dev = "uvicorn app.main:app --reload --workers 1 --host 0.0.0.0 --port 8000"

[packages]
fastapi = "*"
pydantic = "*"
uvicorn = "*"
pytest = "*"
pytest-mock = "*"
requests = "*"
starlette = "*"

[dev-packages]
ipython = "==5.10.0"

[requires]
python_version = "3.9"
